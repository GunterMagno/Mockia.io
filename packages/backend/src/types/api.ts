/**
 * Types Index - Exports backend-specific types only
 * 
 * NOTE: Shared types (SuccessResponse, PaginatedResponse, PermissionLevel, etc.)
 * are now in @mockia/shared for use across backend and frontend.
 * Import those from '@mockia/shared' instead.
 */

export * from './auth';
